[project]
name = "tenant-backup-sync"
version = "0.1.0"
description = "Discover tenant PG databases and register them in Databasus for backup"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "psycopg[binary]>=3.3.4",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
